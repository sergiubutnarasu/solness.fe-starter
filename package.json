{
  "name": "solness.nextjs-starter",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspace @solness/nextjs build",
    "start": "yarn workspace @solness/nextjs start",
    "start:dev": "yarn workspace @solness/nextjs dev",
    "generate": "graphql-codegen"
  },
  "dependencies": {},
  "devDependencies": {
    "@graphql-codegen/cli": "^2.0.1",
    "@graphql-codegen/typescript": "^2.0.0",
    "@graphql-codegen/typescript-operations": "^2.0.1",
    "eslint": "^8.0.1"
  }
}
